    '''open json into list var
       Store New entry in second list var
       Compare keys (material and sizes) of two lists
       If material matches and size matches
       Then add the amount key of new item to
       the amount of already existing item in json

    def sudo():
        json_list = []
        new_list = [User Entered]
        
        json.open(file.json):
            json_list = open(file.json)
        
        for item in json_list[] as i:
            if i[material] == new_list[material]:
                if i[size] == new_list[size]:
                    i[amount] = new_list[amount] + i[amount]
        
        save json_list[]
        
    '''